# MediNova EHR

White-label build of the EHR prototype. Static site — no build step.

- `screens/` — clinician EHR (entry: `screens/login.html`)
- `patient/` — patient portal

Run locally: `npm start` (http-server on :4173) or `python3 -m http.server`.
